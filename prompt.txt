Does the input text semantics or step specify any of the following factors?
Please follow the instructions below and extract the information for each dimensional attribute corresponding to the final target image semantics.

Entity Category and Count: Identify the key foreground entities and their exact quantities.
Exclude background elements or environments; for example, "water" in "swimming in the water" or "mountain" in "a mountain landscape" are not entities.
Output this under "category_number" as a key-value dictionary where keys are entity types (nouns only, no adjectives) and values are integers.

If the exact number is vague (e.g., "some", "several", "a crowd"), set the value to -1.

For image editing tasks: if an object is added, set its final expected quantity; if an object is completely removed, set its quantity to 0.

If unspecified, output null.
example:

<goal>
{
"category_number": {"pig": 2, "dog": 4}
}
</goal>

Reference Consistency: Determine if the prompt explicitly demands that an entity visually matches an external reference image.
Output this under "identity" as a list of dictionaries containing the entity, the image path, and the matching type.
The "type" must be one of: ["face", "pose", "identity"].

"face": Only facial features must match.

"pose": The structural body posture must match.

"identity": The overall visual appearance must be an exact clone.
Note 1: This is only triggered if a specific file path or reference image is explicitly mentioned in the text.
Note 2: For editing tasks, the user's base original image does not count as a reference image for this metric.

If unspecified, output null.
example:

<goal>
{
"identity": [{"dog": "image.jpg", "type": "identity"}]
}
</goal>

Spatial Relationships: Identify explicit physical or spatial placement instructions between distinct entities (e.g., "A is on top of B", "C is positioned to the left of D").
Output this under "position" as a list of strings describing the layout.
Exclude abstract or action-based interactions like "A holding B" or "C looking at D".

If unspecified, output null.
example:

<goal>
{
"position": ["A pig is next to the cow.", "A car is to the right of a bicycle."]
}
</goal>

Attribute Binding: Identify explicit physical traits (such as color, texture, or material) that are directly bound to specific entities.
Output this under "attribute-binding" as a list of descriptive phrases.
The entity must have a clearly stated descriptive adjective (quantities alone do not count).

If unspecified, output null.
example:

<goal>
{
"attribute-binding": ["a red dog", "a wooden mouse", "a tattered backpack"]
}
</goal>

Image Style: Identify if a specific overarching aesthetic, artistic medium, or visual style is requested (e.g., "realistic photography", "anime style", "oil painting", "cyberpunk").
Output this under "style" as a string.

If unspecified, output null.
example:

<goal>
{
"style": "Dreamy watercolor style"
}
</goal>

Background / Environment: Identify the setting, backdrop, or location where the main entities are situated (e.g., "deep ocean", "busy city street", "quiet room").
Output this under "background" as a string.

If unspecified, output null.
example:

<goal>
{
"background": "a quiet room"
}
</goal>

All extracted attributes must be encapsulated within a single JSON object inside <goal> XML tags. If an attribute is missing from the prompt, assign it a null value (do not use string "null").

Format Example for missing items:
<goal>
{
"category_number": {"pig": 2, "dog": 4},
"identity": null,
"position": null,
"attribute-binding": null,
"style": "Dreamy style",
"background": "forest"
}
</goal>

Here is a comprehensive sample reference to guide your output:

<input>
prompt: A dog sits by a round clock under a white lamp, with two green apples and four birds nearby, and the overall environment is in a quiet room. The dog's appearance refers to the image "./temp/test.png".
</input>

<output>
<goal>
{
"category_number": {"dog": 1, "clock": 1, "lamp": 1, "apple": 2, "bird": 4},
"identity": [{"dog": "./temp/test.png", "type": "identity"}],
"position": ["A dog sits by a round clock", "A dog sits under a white lamp"],
"attribute-binding": ["a round clock", "a white lamp", "two green apples"],
"style": null,
"background": "a quiet room"
}
</goal>
</output>

Output the final JSON data in XML format (<goal>...</goal>). Please refer to the provided examples for structural details.
